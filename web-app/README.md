# For The Love Of The Game Web App

Minimal warm-dark browser client for the Darija translator backend.

Run it locally with:

```bash
cd web-app
python3 -m http.server 4173
```

The app expects the backend at `http://127.0.0.1:8081/api`.
