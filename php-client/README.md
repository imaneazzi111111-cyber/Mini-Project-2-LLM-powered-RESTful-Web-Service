# PHP Client Placeholder

This folder is reserved for the PHP client deliverable required by the assignment.

Planned tasks:

- call the secured `TranslatorResource` endpoint
- submit text and render the Darija response
- mirror the same prompt and response flow used by the web client
